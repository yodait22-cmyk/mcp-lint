[
  {
    "name": "broken-tool",
    "inputSchema": {
      "type": "object",
      "properties": {
        "query": { "type": "string", "required": false },
        "encoding": { "type": "string", "contentEncoding": "base64" },
        "status": { "type": "string", "enum": [] }
      }
    }
  }
]
